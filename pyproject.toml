[project]
name = "interpreter"
version = "0.2.0"
description = "Real-time meeting transcription and interpretation assistant"
readme = "README.md"
authors = [{ name = "Chenfeng Chen" }]
requires-python = ">=3.14"
license = { text = "MIT" }
keywords = ["ai", "interpreter", "transcription", "stt", "translation"]
dependencies = [
    "huggingface-hub>=1.28.0",
    "jiwer>=4.0.0",
    "matplotlib>=3.11.1",
    "noisereduce>=3.0.3",
    "numpy>=2.5.2",
    "onnxruntime==1.24.4",
    "sacrebleu>=2.6.0",
    "sentencepiece>=0.2.2",
    "sherpa-onnx==1.13.0",
    "sounddevice>=0.5.6",
    "soundfile>=0.14.0",
    "torch>=2.13.0",
    "torchaudio>=2.11.0",
    "transformers>=5.15.1",
]

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "prek>=0.4.11",
    "pytest>=9.1.1",
    "ruff>=0.16.0",
    "ty>=0.0.63",
]

[tool.uv.sources]
torch = [
  { index = "pytorch-cpu", marker = "sys_platform == 'linux'" },
]
torchaudio = [
  { index = "pytorch-cpu", marker = "sys_platform == 'linux'" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.pytest]
testpaths = ["tests"]
